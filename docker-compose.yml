services:
  tasknest:
    build: .
    ports:
      - "127.0.0.1:8000:8000"

    env_file:
      - .env

    environment:
      HOST: "0.0.0.0"
      PORT: "8000"
      TASKNEST_DATA: "/app/data"

    volumes:
      - tasknest_data:/app/data

    restart: unless-stopped

volumes:
  tasknest_data: